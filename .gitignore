# Python
__pycache__/
*.pyc

# 运行时生成状态（首次运行自动重建，不入库）
skill/scripts/models_registry.json
skill/scripts/openrouter_models_cache.json

# 核验产物（可再生，不入库）
score_verification_report.json
verify_pending.json
backup_scores.json
render_scores.json
.verify_cache/

# 生成的天梯榜工作簿（可再生交付物，不入库）
*.xlsx
# 会话内部计划文档
.zcode/

# 渲染调试转储
_*_rendered.txt

# 发现产物
hot20.json
