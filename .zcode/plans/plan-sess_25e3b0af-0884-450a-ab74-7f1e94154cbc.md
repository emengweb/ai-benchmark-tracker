1. **Make export scope and company matching correct**
   - Extend `skill/scripts/export_benchmark_excel.py` with canonical institution aliases and region classification derived from the institution/provider name. Keep display names unchanged, but normalize aliases such as Chinese/English Z.ai, Alibaba/Qwen, Moonshot/Kimi, Tencent/Hunyuan, Xiaomi/MiMo, OpenAI, Anthropic, Google, NVIDIA, Poolside, and Upstage.
   - Add `scope=all|domestic|international` and a separate institution/company filter to the exporter and CLI. A request for domestic models filters by classified institution region; a request for a named company matches canonical aliases. Apply both filters before scoring, sorting, and rank assignment.
   - Normalize legacy registries on read (existing records have no region field), infer only from known institution aliases, and mark unknown institutions `unknown`. Preserve explicit valid regions. Use the full normalized model name as the registry merge key so discovered variants from the same provider cannot overwrite one another.
   - Support incomplete runtime records without crashing: validate numeric metric values, compute the composite only when GPQA Diamond, SWE-bench Verified, and MMLU-Pro are all present and in range, rank complete records only, and show incomplete records as unranked with an explicit status in the workbook. Never substitute SWE-bench Pro, Artificial Analysis indexes, or another metric.
   - Make filename, title, subtitle, selected rows, rank population, and source-footnote index reflect the active scope/company. Replace the all-model hard-coded footnote output with entries derived from selected records, retaining known descriptions and adding transparent generic entries for newly discovered source links.

2. **Add executable runtime model discovery**
   - Create a stdlib-based `skill/scripts/discover_models.py` adapter that fetches `https://openrouter.ai/api/v1/models` at runtime with timeout, retries, `Retry-After` handling, and a cache/last-success fallback. Parse provider/model identifiers, display name, release timestamp, modalities, context, pricing converted from per-token to `$ / 1M`, canonical model URL, and discovery timestamp.
   - Add optional parsing of the user-provided OpenRouter rankings page as a popularity hint, but treat it as best-effort because it is browser-rendered and has no stable documented ranking schema. If ranking rows cannot be extracted, fall back to a clearly labeled deterministic API catalog ordering (latest/available candidates), never claim it is a monthly top ranking.
   - Apply the same institution-derived region and company matching as the exporter, support a configurable candidate limit (default around 20), and emit normalized JSON records suitable for `--add-model`. Newly discovered records carry `region`, discovery provenance, and missing benchmark fields rather than fabricated scores.
   - Document that this adapter discovers model metadata; benchmark scores still require per-model evidence retrieval from official system cards/benchmark result pages or reproducible evaluators. Dynamic source failures must be reported and must not silently overwrite verified scores.

3. **Expand the skill’s runtime orchestration instructions**
   - Update `skill/SKILL.md` scope resolution: `国内/国产/中国` maps to domestic, `国际/海外` to international, `国内外/全球` or no geographic restriction to all; an explicit company request adds the company filter and must never fall back to all models.
   - Replace the current static-only Stage A/B guidance with an executable workflow: run `discover_models.py` for the requested region/company, inspect OpenRouter ranking/API provenance, then search each selected model’s official and independent Benchmark evidence. Record exact model version, benchmark variant, score, source URL, publication/retrieval time, and protocol; retain unknown/incomplete metrics as unranked.
   - Add commands showing domestic-only, company-only (for example OpenAI), and default popular/latest discovery followed by export. Include the no-fabrication, source precedence, cache/fallback, and freshness rules.
   - Keep the existing Excel column contract and current-working-directory output rule, and make Google Drive upload explicitly optional after local verification.

4. **Update project references and README**
   - Add OpenRouter Models API and rankings-page limitations to `skill/references/datasources.md`.
   - Update `README.md` with region/company CLI options, discovery command, JSON record shape, runtime Benchmark evidence workflow, incomplete-score behavior, and examples for domestic, OpenAI-only, and unrestricted reports.

5. **Synchronize and verify**
   - Inspect the existing installation targets, then copy the updated `skill/` contents to Codex, Claude, Pi, Zcode, and the discovered AionUi user-skill directory, preserving the destination name `ai-benchmark-tracker`. Do not stop, restart, or otherwise terminate any AionUi process.
   - Run local exporter checks for domestic, international, company-only, and all scopes. Run the discovery adapter against OpenRouter when network access permits; otherwise verify its deterministic/cache error path without claiming fresh data.
   - Inspect generated workbooks with `openpyxl`: domestic output contains only classified Chinese institutions and domestic footnotes, OpenAI output contains only OpenAI rows, ranks restart at 1 for complete records, incomplete rows are clearly unranked, and unrestricted output retains all available records. Report exact output paths and any source/network limitations.